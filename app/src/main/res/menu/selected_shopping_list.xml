<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:Housync="http://schemas.android.com/apk/res-auto"
    xmlns:app="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="?attr/actionBarSize"
    android:background="?attr/colorPrimary"
    app:popupTheme="@style/AppTheme.PopupOverlay">
    <item
        android:id="@+id/delete_selected_item_shopping_list"
        android:title="Delete Item"
        Housync:showAsAction="ifRoom"
        android:icon="@drawable/ic_delete"
        android:orderInCategory="1"
        />
    <item
        android:id="@+id/check_selected_item_shopping_list"
        android:title="Check Item"
        Housync:showAsAction="ifRoom"
        android:icon="@drawable/ic_shopping_cart"
        android:orderInCategory="2"
        />
</menu>